export const metadata = { title: "HeartLink Pro" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="max-w-md mx-auto p-4 bg-white shadow">{children}</body>
    </html>
  );
}